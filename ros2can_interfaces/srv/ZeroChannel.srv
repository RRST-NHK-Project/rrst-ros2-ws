# 指定デバイス(device_id)の指定チャンネル(channel_index)について、
# マイコン側の値は変更せず、ros2can GUI側が保持するソフトウェアオフセットのみを
# 「現在値」に設定する(以後 serial_rx_[ID]_zeroed / GUI表示は現在値からの相対値になる)。
# channel_index に -1 を指定すると、そのデバイスの全24チャンネルを一括で原点セットする。

int32 device_id
int32 channel_index
---
bool success
string message
